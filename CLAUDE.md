# CLAUDE.md(在庫管理_本番用)

スーツケース救急車の在庫管理Webアプリ(kintone連携)。別PC・別アカウントでこのプロジェクトを開く
Claude Codeセッションのための引き継ぎ情報。ここに書いた内容は、このアカウントの自動メモリとは
別に、プロジェクトフォルダそのものに残しているため、誰が・どのPCで開いても読める。

## 概要

- 目的: 店舗(スーツケース救急車・豊田倉庫)の在庫をkintoneのデータと連携して閲覧し、出庫・入庫を
  登録する。あわせて、提携店(スーツケース修理王・ミニット)からのパーツ購入依頼を管理する。
- 利用者: スーツケース修理業のスタッフ(非エンジニア)。説明・ドキュメントは平易な日本語で書く。
- 操作マニュアル: `在庫管理アプリ_操作マニュアル.html`/`.pdf`(実画面のスクリーンショット付き)。
  画面の挙動を人に説明する際は、このPDFの記憶ではなく`server.js`/`www/app.js`のコードを毎回
  確認してから書く(見た目や名前からの推測で書かない)。

## 構成

- `server.js`: Expressのルーティング、Basic認証によるログイン
- `kintone.js`: kintone REST APIとの通信(在庫計算、出庫・入庫登録、提携店依頼の作成・処理、
  PDF生成の呼び出し)。各kintoneアプリの役割・フィールド設計の注意点はファイル冒頭のコメント参照
- `pdf.js`: 納品書・請求書PDFの組み立て(pdfmake)
- `www/`: フロントエンド(index.html / login.html / app.js / style.css / notice-panel.js)
- `.env.example`: 必要な環境変数の一覧と説明(実際の値は`.env`。Gitには含めない)
- `knowledge/お知らせ.md`: アプリ上部の「お知らせ」パネルに出る更新履歴。新しい項目を上に
  `## YYYY-MM-DD タイトル`の見出し＋本文で追記していく(`GET /api/notices`がこれを読んで返す)。
  line-reply-assistantの同名の仕組みを踏襲したもので、既読管理はブラウザのlocalStorage
  (キー`zaiko_notice_seen`)。新機能を追加したら、このファイルにも1項目書き足すこと。

## 受付アシスタント(line-reply-assistant)からのシングルサインオン

このアプリは受付アシスタント(line-reply-assistant)のホーム画面・受信箱からiframeで開かれる
運用を想定している(社内向けポータル統合の一環、別タブに遷移させない方針)。`server.js`に
以下の仕組みが入っている。

- 未ログイン状態でこのアプリを直接開くと、環境変数`RECEPTION_LOGIN_URL`が設定されていれば
  自前のログイン画面の代わりに受付アシスタントのログイン画面(`<URL>/login?next=zaiko`)へ
  リダイレクトする。受付アシスタント側でログインすると、短命(5分)の署名付きトークンを
  `?sso=`に付けてこのアプリへ自動的に戻してくれる(`src/zaikoSso.js`、line-reply-assistant側)。
  このアプリはトークンを検証できたら、通常ログインと同じセッションCookie(`zaiko_session`)を
  自動発行する。
- トークンの検証には環境変数`SSO_SHARED_SECRET`(ランダムな共有シークレット)を使う。
  **line-reply-assistant側の`.env`の`SSO_SHARED_SECRET`と完全に同じ値にする必要がある**
  (Renderでは両サービスのダッシュボードにそれぞれ同じ値を登録する)。
- `RECEPTION_LOGIN_URL`/`SSO_SHARED_SECRET`のどちらかが未設定なら、この仕組みは働かず
  従来通りこのアプリ自身のログイン画面(`BASIC_AUTH_USER`/`PASSWORD`)が使われる。自前の
  `/login`ページ自体は削除しておらず、URLを直接開けば受付アシスタントを経由せず今まで通り
  ログインできる(受付アシスタント側に障害があった場合の抜け道として残してある)。

## 環境変数・データについて

- ローカルの`.env`は本番のkintoneアプリに直結しており、開発用の別環境は無い。ローカルで動かす
  だけでも本番データを直接読み書きすることになるため、出庫・入庫登録などの書き込み系操作を
  試す際は必ず注意し、テストで作ったレコードは後片付けする。
- kintoneのAPIトークンに設定できる権限は5つ(閲覧・追加・編集・削除・コメント)のみで、ファイルの
  読み書き権限はトークンには付けられない。添付ファイルが絡む操作は`KINTONE_SERVICE_LOGIN_ID`/
  `KINTONE_SERVICE_PASSWORD`(実在ユーザーのパスワード認証)を併用している(`kintone.js`参照)。

## デプロイ・Git運用

- GitHub(`inagakitakeshi1025-tech/suitcase-zaiko`、共有アカウント)+ Render で運用。`main`への
  pushでRenderが自動デプロイする。Render側の環境変数は`.env`と同じ内容をダッシュボードの
  Environment Variablesに登録している。
- commitは内容を確認した上でClaudeが作成してよい。pushは実行前に一声かける。
- pushが実際に反映されたかは`git fetch origin main && git log origin/main -1 --oneline`で確認する
  (「pushした」という口頭・スクリーンショットの報告だけを鵜呑みにしない。別セッション・別時点の
  結果を今回の結果として提示されたことがあるため)。

## kintoneの専門知識が無い担当者でも開発を続けられるように

このプロジェクトを引き継ぐ担当者は、kintoneに触れたことが無い可能性がある(アプリ・フィールド・
サブテーブル・ルックアップ・APIトークンの権限といった概念に馴染みが無い)。フィールド名や型が
分からないことを都度その人に確認させると、本人もkintone管理画面を開いて調べる必要が生じ、
負担が大きい。**フィールド名や実際のデータについて分からないことがあれば、人に聞く前にまず
以下のスクリプトでkintone本体に直接問い合わせて自己解決を試みること。**

```
node scripts/kintone-fields.js              # 7アプリ全部のフィールド構成(コード・型・ラベル)を表示
node scripts/kintone-fields.js ZAIKO        # 指定したアプリだけ表示
node scripts/kintone-search.js STORE        # そのアプリの実データを最大10件、閲覧のみで確認
node scripts/kintone-search.js ZAIKO '部品番号 like "P-101"'   # 条件付き検索(閲覧のみ・書き込みなし)
```

いずれも`.env`の既存トークン(閲覧権限)で動く読み取り専用ツールで、書き込みは一切行わない。

対象アプリ一覧(kintone側の名称 / このプロジェクトでのキー):

| キー | kintone側の名称 | 用途 |
|---|---|---|
| ZAIKO | 在庫管理アプリ | 在庫一覧の閲覧のみ(書き込みは出庫・入庫管理アプリ経由) |
| SHUKKO | 出庫管理アプリ | 出庫登録の書き込み先 |
| NYUKO | 入庫管理アプリ | 入庫登録の書き込み先 |
| PARTS | パーツ管理アプリ | 商品マスタ(パーツ番号・名称等)。閲覧のみ |
| STORE | 店舗マスタアプリ | 出庫元・入庫先の店舗情報。閲覧のみ |
| IRAI | 提携店からの依頼アプリ(スーツケース修理王向け) | 依頼の閲覧・追加 |
| IRAI_MINUTE | 提携店からの依頼アプリ(ミニット向け) | 依頼の閲覧・追加 |

**それでも自己解決できない、kintone管理画面の操作そのものが必要なケース**(新しいフィールド・
アプリの追加、APIトークンの発行・権限変更など)は、上記スクリプトでは対応できない。この
プロジェクトの担当者はkintoneの管理者アカウントを確認できるため、その操作自体は担当者自身で
行える想定。ただし何を・どう設定すべきか(権限の選び方、フィールドの型など)判断に迷う場合は、
「業務アプリ開発」スキルや`kintone-api-token-permissions`の知見(APIトークンに設定できる権限は
「閲覧・追加・編集・削除・コメント」の5つのみで、ファイルの読み書き権限はトークンには付けられ
ない)を踏まえて助言すること。添付ファイルが絡む書き込みには`KINTONE_SERVICE_LOGIN_ID`/
`_PASSWORD`のパスワード認証を併用している(上記「環境変数・データについて」参照)。

## 過去の作業で分かった注意点

- `www/app.js`の`renderHistory()`(登録履歴)と`renderRequests()`(提携店からの依頼)は、画面上は
  別領域なのにCSSクラス名(`.request-card`など)が一部重複している。DOM操作・自動テストで
  セレクタを使う際は親要素(`#requests-col`等)で必ずスコープすること。
- 出庫・入庫登録の「🎤 音声入力」機能(`www/app.js`末尾の`handleVoiceUtterance`まわり)は、
  ブラウザ標準のWeb Speech API(`SpeechRecognition`/`webkitSpeechRecognition`)を使っており、
  Anthropic APIも外部の音声認識サービスも使っていない(追加コストは発生しない)。ただし
  **iPhoneのSafariは対応が不安定**なため、非対応時は自動でその旨を表示し、従来通りの検索・
  バーコード読取に誘導するだけにしてある。誤登録防止のため、「保存」と話しても自動送信はせず
  対象の登録ボタンを画面上でハイライトするだけにとどめており、実際の送信は必ず人がボタンを押す。
  パーツ番号は末尾が数字であることが多く(例:`O-199`)、話した数量の数字とテキスト上でくっつく
  ことがある(`O-1992個`のように)。そのため実装では、数量を抜き出す前にまず登録済みのパーツ
  番号がテキストに含まれていないかを先に探し(`findPartNoInText`、一致する番号のうち最長のもの
  を優先)、番号が確定してから残りの文字列を数量として解釈する順序にしてある。この順序を逆にする
  と(先に数量を正規表現で抜いてしまうと)パーツ番号の一部が数量に食われて検索に失敗するバグに
  なるため、修正時は注意すること。
  実機での動作確認で判明した追加の癖が2つある。①パーツ番号中のハイフンは、音声だと「O199」の
  ように省略されたり「Oの199」のように「の」に化けたりすることが多いため、`findPartNoInText`は
  ハイフン相当の記号・「の」を無視した`stripPartNoSeparators`で比較している(番号確定後に残りを
  数量として解釈する際も、同じ除去をしてから計算しないと番号の一部が数量解析に紛れ込む)。②「えー
  はいふんいちまるいち」のように区切って話すと、ブラウザの音声認識は単語ごとに別々の確定結果
  (isFinal)を返すことがあり、都度即座に解析すると「番号」と「個数」がバラバラに処理されて
  噛み合わなくなる。そのため確定結果は即座に処理せず、`VOICE_DEBOUNCE_MS`(1.2秒)発話が
  途切れるまで`voiceBuffer`に溜めてからまとめて解析している。
  もう一つ、コード側の事後補正では直しきれない癖として、アルファベット1文字だけを単独で
  発音すると(特に「イー」など)、ブラウザの音声認識が全く別の日本語の単語として空耳変換して
  しまうことがある(実機で「イー、ハイフン、ヒャクイチ」と区切って話すと聞き取り不能な結果に
  なったが、「イーヒャクイチ」のように区切らず数字まで一気に話すと正しく認識できた、という
  報告あり)。これは音声認識エンジン側の言語モデルの癖なので、`www/index.html`の音声入力欄の
  ヒントで「番号の数字までひと息で続けて話す」よう案内する形で対応している。同様の空耳が他の
  アルファベットでも頻発するようなら、まずヒント文の追記で様子を見て、それでも改善しなければ
  個別の空耳パターン→正しい文字の変換テーブルを足すことを検討する。
- 操作マニュアルPDF(Microsoft Edgeのヘッドレスモードで印刷)で`.page-flex`(文章+スクリーン
  ショットの横並び)を使う場合、1ページに収まらないと画像だけ次ページに孤立するなど崩れやすい。
  生成後は必ず全ページを目視確認し、崩れていれば組(文章+画像)ごとに独立した`.page`へ分割する。

## 新しいPC・アカウントでのセットアップ

1. Node.js(18以上)をインストール
2. プロジェクトフォルダを配置したら、フォルダ内で `npm install`
3. `.env`が無い場合は`.env.example`を参考に用意する(USB等で引き継いだ場合は既にあるはず)
4. `npm start` で起動確認(`http://localhost:<.envのPORT>`)
5. GitHubへのpushには、共有アカウントの認証情報が必要(git credential managerに未設定なら
   初回push時に求められる)
