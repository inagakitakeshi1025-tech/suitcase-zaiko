# kintone接続設定(在庫管理Web化用)
# このファイルを「.env」という名前でコピーし、実際の値を入力してください(ローカル動作確認用)。
# Render本番環境では、この内容を Render ダッシュボードの Environment Variables に登録する。
# .env は秘密情報(APIトークン)を含むため、他人と共有・Gitにコミットしないこと。

# 例: https://your-subdomain.cybozu.com
KINTONE_BASE_URL=https://your-subdomain.cybozu.com

# 在庫管理アプリ(一覧の閲覧のみ。書き込みは出庫管理・入庫管理アプリ経由)
KINTONE_ZAIKO_APP_ID=
KINTONE_ZAIKO_API_TOKEN=

# 出庫管理アプリ(レコード追加が必要。権限「レコード閲覧・追加」)
KINTONE_SHUKKO_APP_ID=
KINTONE_SHUKKO_API_TOKEN=

# 入庫管理アプリ(レコード追加が必要。権限「レコード閲覧・追加」)
KINTONE_NYUKO_APP_ID=
KINTONE_NYUKO_API_TOKEN=

# パーツ管理アプリ(商品マスタ。閲覧のみでよい。権限「レコード閲覧」)
KINTONE_PARTS_APP_ID=
KINTONE_PARTS_API_TOKEN=

# 店舗マスタアプリ(出庫元・入庫先のルックアップ参照先。閲覧のみでよい。権限「レコード閲覧」)
KINTONE_STORE_APP_ID=
KINTONE_STORE_API_TOKEN=

# 提携店からの依頼アプリ(スーツケース修理王向け。レコード閲覧・追加が必要)
KINTONE_IRAI_APP_ID=
KINTONE_IRAI_API_TOKEN=

# 提携店からの依頼アプリ(ミニット向け。レコード閲覧・追加が必要)
KINTONE_IRAI_MINUTE_APP_ID=
KINTONE_IRAI_MINUTE_API_TOKEN=

# 依頼表(IRAI/IRAI_MINUTE)への書き込み専用。APIトークン経由の書き込みはEveryone扱いで
# 権限が無いため反映されない。そのため権限を持つ実在のkintoneユーザー(サービスアカウント)の
# ID・パスワードをここに設定し、パスワード認証を併用する。省略可(未設定でも他機能は動く)。
KINTONE_SERVICE_LOGIN_ID=
KINTONE_SERVICE_PASSWORD=

# このアプリ自体のログイン用。URLとこのID・パスワードを知っている人だけが使える。
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

# ログインセッションの署名鍵(省略可。未設定の場合はBASIC_AUTH_PASSWORDから自動生成される)。
# 設定する場合はランダムな長い文字列を入れる。
SESSION_SECRET=

# 受付アシスタント(line-reply-assistant)からのシングルサインオン用(省略可)。
# 両方に設定すると、①在庫管理アプリを直接開いて未ログインの場合は受付アシスタントの
# ログイン画面に飛ぶ(自前のログイン画面は使わなくなる)、②受付アシスタント側でログイン
# 済みならiframe経由で在庫管理アプリを開いたときログイン不要になる。どちらも未設定でよく、
# その場合は従来通りこのアプリ自身のログイン画面(BASIC_AUTH_USER/PASSWORD)が使われる。
# 2つの値は line-reply-assistant の .env と完全に同じ文字列にする必要がある。
RECEPTION_LOGIN_URL=https://line-reply-assistant.onrender.com
SSO_SHARED_SECRET=

# ローカル動作確認時のポート番号(Render本番では自動で割り当てられるため通常は不要)
PORT=8080
